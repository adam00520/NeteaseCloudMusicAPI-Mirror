name: Ping Render Service

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping service
        run: curl --retry 3 --retry-delay 5 -s https://neteasecloudmusicapi-mirror.onrender.com > /dev/null
